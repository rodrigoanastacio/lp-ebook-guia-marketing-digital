import styles from '../styles/components/Headline.module.scss'

const Headline = () => {
  return (
    <h1 className={styles.title}>
      O Marketing Digital não precisa ser complicado!
    </h1>
  )
}

export default Headline